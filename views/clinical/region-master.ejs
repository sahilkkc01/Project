<!doctype html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

  <title>LifeLinker</title>
 
<%- include('../header'); -%>
    <!-- main content start -->
    <div class="main-content">

      <div class="row content-top-gap2">
        <div class="col-lg-12">
          <nav class="navbar navbar-expand-lg navbar-light bg-white">
            <a class="navbar-brand text-center mr-5" href="#">
              <span class="dash-icon"><i class="fas fa-tachometer-alt"></i></span>
              Dashboard
            </a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
              aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
              <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
              <ul class="navbar-nav">
                <li class="nav-item active text-center mr-5">
                  <a class="nav-link" href="#">
                    <span class="dash-icon"><i class="far fa-user"></i></span>
                    Master
                  </a>
                </li>
                <li class="nav-item text-center mr-5">
                  <a class="nav-link" href="#">
                    <span class="dash-icon"><i class="fas fa-exchange-alt"></i></span>
                    Transaction
                  </a>
                </li>
                <li class="nav-item text-center mr-5">
                  <a class="nav-link" href="#">
                    <span class="dash-icon"><i class="fas fa-hand-holding-medical"></i></span>
                    Patient 360
                  </a>
                </li>
                <li class="nav-item text-center mr-5">
                  <a class="nav-link" href="#">
                    <span class="dash-icon"><i class="fas fa-receipt"></i></span>
                    Outside Reciept
                  </a>
                </li>
                <li class="nav-item text-center mr-5">
                  <a class="nav-link" href="#">
                    <span class="dash-icon"><i class="fas fa-file-invoice"></i></span>
                    Outpatient Department
                  </a>
                </li>
                <li class="nav-item text-center mr-5">
                  <a class="nav-link" href="#">
                    <span class="dash-icon"><i class="fas fa-chart-line"></i></span>
                    Analytics
                  </a>
                </li>
                <li class="nav-item text-center mr-5">
                  <a class="nav-link" href="#">
                    <span class="dash-icon"><i class="far fa-clock"></i></span>
                    Waiting List
                  </a>
                </li>
                <li class="nav-item text-center mr-5">
                  <a class="nav-link" href="#">
                    <span class="dash-icon"><i class="fas fa-wrench"></i></span>
                    Adminstration
                  </a>
                </li>
                <li class="nav-item text-center">
                  <a class="nav-link" href="#">
                    <span class="dash-icon"><i class="fas fa-spinner"></i></span>
                    Additional
                  </a>
                </li>

              </ul>
            </div>
          </nav>
        </div>
      </div>
      <!-- content -->
      <div class="container-fluid">

        <div class="chart">
          <div class="row">
            <div class="col-lg-12 pl-lg-2 mt-2">
              <h4 class="primary">Region Master</h4>
            </div>
            <div class="col-lg-12 pl-lg-2 chart-grid mt-1">

              <!-- form start here -->
               <div id="errordiv1"></div>
              <form id="region_master">
                <input type="hidden" name="query" id="query" value="0">
      <input type="hidden" name="itemid" id="itemid" value='<%= a.id %>'>
                <div class="card card_border">
                  <div class="">
                    <!--Past your code here starts-->
                    <div class="col-xl-12 p-heading">
                      <h6 class="p-text">REGION MASTER</h6>
                    </div>
                    <div class="card-body">
                      <div class="form-row">
                        <div class="col-3 mb-3 mb-3">
                          <div class="form-group"><label class="input__label">Code </label><input type="text"
                              class="form-control input-style" id="region_m_code" name="region_m_code" class="form-control input-style" 
                              data-validation="required" data-error-message="Code is required." placeholder=""></div>
                        </div>
						<div class="col-3 mb-3 mb-3">
                          <div class="form-group"><label class="input__label">Region Name </label><input type="text"
                              class="form-control input-style" id="region_m_name" name="region_m_name" class="form-control input-style"
                               data-validation="required" data-error-message="Region name is required." placeholder=""></div>
                        </div>
						<div class="col-3 mb-3 mb-3">
                          <div class="form-group"><label class="input__label">Country </label>
                            <select id="country_id" name="country_id" class="form-control input-style" 
                            data-validation="required" data-error-message="Country is required.">
                            <option value="">Select</option>
                            <option value="INDIA">INDIA</option>
                            <!-- Options will be populated via AJAX -->
                        </select></div>
                        </div>
						<div class="col-3 mb-3 mb-3">
                          <div class="form-group"><label class="input__label">State </label> 
                            <select id="state_id" name="state_id" onchange="loadCity(this)" class="form-control input-style" data-validation="required" data-error-message="State is required.">
                            <option value="">Select State</option>
                            <!-- Options will be populated via AJAX -->
                        </select></div>
                        </div>
						<div class="col-3 mb-3 mb-3">
                          <div class="form-group"><label class="input__label">Emirate/City </label>
                            <select id="city_id" name="city_id" class="form-control input-style" data-validation="required" data-error-message="City is required.">
                            
                            <!-- Options will be populated via AJAX -->
                        </select></div>
                        </div>
						<div class="col-3 mb-3 mb-3">
                          <div class="form-group"><label class="input__label">Pin Code </label><input type="number"
                              class="form-control input-style" id="region_m_pincode" name="region_m_pincode" class="form-control input-style" data-validation="required" data-error-message="Pincode is required." placeholder=""></div>
                        </div>
                      </div>
                    </div>

                    <!--Past your code here ends-->
                  </div>
                </div>
                <!-- buttons-->
                <div class="text-right col-lg-12 mt-2">
                  <button type="button" class="btn-success btn-style btn mr-2"  disabled id="mod-btn" onclick="updateQuery()"><strong><i
                    class="fa-regular fa-pen-to-square" ></i> Modify</strong></button>
            
            
                        <button type="submit" id="sub-btn" class="btn-primary btn-style btn"><strong><i
                            class="far fa-save"></i> Save</strong></button>
                            <button type="button"  class="btn btn-danger btn-style ml-2 mr-3" onclick="window.location.href = '/clinic/21';"><strong><i class="fas fa-times"></i> Cancel</strong></button>
                </div>
              </form>
            </div>
            <!--footer section start-->
          <%- include('../footer'); -%>


          <script>
             function updateQuery(){
        document.getElementById('query').value=1;
        $('#mod-btn').prop('disabled', true);   
        $('input[data-validation], select[data-validation]').prop('disabled', false);
        $('#region_m_code').prop('disabled', true);
        document.getElementById('sub-btn').disabled = false;
      }
      if('<%= a.id %>'!==''){
        $('input[data-validation], select[data-validation]').prop('disabled', true);
        document.getElementById('sub-btn').disabled = true;
        document.getElementById('mod-btn').disabled = false;
        
      }else{
      
        document.getElementById('mod-btn').disabled = true;
      }
      if('<%= a.status %>'!=='true'){
        document.getElementById('mod-btn').disabled = true;
      }
                  
            document.getElementById('region_m_code').value = '<%= a.region_m_code %>';
            document.getElementById('region_m_name').value = '<%= a.region_m_name %>';
            document.getElementById('country_id').value = '<%= a.country_id %>';
   
            document.getElementById('region_m_pincode').value = '<%= a.region_m_pincode %>';
        </script>

          <script>
             function loadCity(a) {
  $.ajax({
    url: '/clinic/cities', // Endpoint to fetch all cities
    type: 'GET',
    success: function(data) {
      console.log(data);
      
      let selectOptions = '<option value="">Select city</option>'; // Initial select option
      
      // Filter cities based on selected stateCode
      const filteredCities = data.filter(city => city.stateCode === a.value);
      
      filteredCities.forEach(function(city) {
        selectOptions += `
          <option value="${city.cityName}">${city.cityName}</option>
        `;
      });
      
      $('#city_id').html(selectOptions); // Populate select options
    },
    error: function(xhr, status, error) {
      console.error('Error fetching cities:', error);
    }
  });
}

          </script>

<script type="module">
 
  import { validateForm } from '../javascripts/validation.js';
  $(document).ready(function() {

    
    function loadState(a){
            fetch('/clinic/reloadState')
                .then(response => response.json())
                .then(data => {
                    const dropdown = document.getElementById(a);
                    data.forEach(state => {
                        const option = document.createElement('option');
                        option.value = state.stateCode;
                        option.textContent = state.stateName;
                        dropdown.appendChild(option);
                        
                    });
                    dropdown.value = '<%= a.state_id %>';
                })
                .catch(error => console.error('Error fetching states:', error));
            }
	       loadState('state_id');



      console.log("Document is ready"); // Debugging log
     

      // const {isValid}=validateForm('#region_master')
      // alert(isValid)
      // console(isValid);
      $('#region_master').on('submit', function(event) {
        event.preventDefault()
        const {isValid,errorMessage,formData}=validateForm('#region_master')
    
      const errorDivId = '#errordiv1'

      if (isValid) {
                  $.ajax({
                      url: '/clinic/region-master-f',
                      type: 'POST',
                      contentType: 'application/json',
                      data: JSON.stringify(formData),
                      success: function(response) {
                          alert(response.msg);
                          $(errorDivId).removeClass('alert-danger').addClass('alert-success').text(response.msg).show();
                          console.log('Response:', response);
                      },
                      error: function(xhr, status, error) {
                        const response = JSON.parse(xhr.responseText);
                        $(errorDivId).removeClass('alert-success').addClass('alert-danger').text(response.msg).show();
                          console.error('Error:', error);
                      }
                  });
              } else {
                  alert(errorMessage);
                  $(errorDivId).removeClass('alert-success').addClass('alert-danger').text(errorMessage).show();
                  // $(errorDivId).text(errorMessage).show();
              }
            })

      
  });
</script>
